class swipeCardWidget extends StatelessWidget{
  const swipeCardWidget ({super.key})
  final CardSwiperController controller = CardSwiperController();
  final cards = greens.map()

  @override
  Widget build(BuildContext context){
    return Scaffold(

    )
  }
}